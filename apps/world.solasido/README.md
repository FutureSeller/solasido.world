# world.solasido

Vite + React + Cloudflare Pages app.

## Scripts

- `pnpm dev` — Vite dev server
- `pnpm build` — type-check and build to `dist/`
- `pnpm preview` — preview the built output with `wrangler pages dev`
- `pnpm deploy` — build and deploy to Cloudflare Pages (`world-solasido`)
